# Bitacora de sesion — #238 listado de lotes activos

## Recursos

- Tokens: no informado.
- Comandos: `npm ci`, `npm run prisma:generate`, `npm run lint`, `npm run typecheck`, `npm run test:integration`.
- Integracion verde: 15 archivos, 47 tests.

## Decisiones

- La ruta de activos devuelve una lista: el modelo permite mas de un lote activo
  por unidad y este issue no define cual elegir.
- Las lecturas se hacen con `withTenant`, inclusive la verificacion de empresa,
  para que RLS sea parte del resultado y no un filtro opcional de TypeScript.

## Prueba negativa

Se cambio temporalmente la expectativa para admitir un solo lote activo. La
integracion quedo roja: recibio dos ids activos cuando esperaba uno. Restaurada
la expectativa de ambos, el archivo paso con 4 tests.

## Fuera de alcance

- No hay migraciones ni cambios de schema.
- No se crean, editan ni eliminan unidades o lotes.
- Falta una regla de negocio si se requiere un unico lote activo por unidad.
